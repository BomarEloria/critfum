import React from "react";
import ReactDOM from "react-dom/client";
import WeaponFumbleApp from "./WeaponFumbleApp";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <WeaponFumbleApp />
  </React.StrictMode>
);
